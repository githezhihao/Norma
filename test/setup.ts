// ============================================================
// Vitest 全局测试配置
// ============================================================

import { afterAll } from 'vitest';

// 全局清理钩子
afterAll(() => {
  // 清理全局资源（如果有）
});
